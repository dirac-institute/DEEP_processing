description: BIAS calibration construction for DEEP
instrument: lsst.obs.decam.DarkEnergyCamera
imports:
  - location: $CP_PIPE_DIR/pipelines/DECam/cpBias.yaml

subsets:
  step1:
    subset:
    - cpBiasIsr
  step2:
    subset:
    - cpBiasCombine
